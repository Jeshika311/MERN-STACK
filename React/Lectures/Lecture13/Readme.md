useRef 
A useCase :- variable persist value across re render
when re render then normal variable fhir se initialise hoga but useref ki help se create kiye hue variables apni value ko persist karte hai across re render

State variable -> Change -> re render UI
useRef -> Change -> No re render 

B useCase :- useRef is made so that we can direct change any element from DOM

Steps :-
Create Reference
link Reference
access current field of Reference