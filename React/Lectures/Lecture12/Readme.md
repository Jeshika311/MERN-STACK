<!-- Redux Toolkit
it solves the problem of prop drilling by creating a centralised store

Some terms

Action :- it is nothing it is just a wrraper over the event

Reducer :- they are just functions that contains the logic to manipulate the state

State :- state is data tha we want to manage(access , update,delete)

Slice :- its a feature where we maintain initial state of that feature and entire logic represented in terms of function to update the state

Store:- single source of truth where state is managed -->